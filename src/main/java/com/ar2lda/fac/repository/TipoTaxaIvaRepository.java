package com.ar2lda.fac.repository;

import com.ar2lda.fac.model.TipoTaxaIva;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TipoTaxaIvaRepository extends JpaRepository<TipoTaxaIva, String> {
}
