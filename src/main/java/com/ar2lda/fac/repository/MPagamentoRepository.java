package com.ar2lda.fac.repository;

import com.ar2lda.fac.model.MPagamento;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MPagamentoRepository extends JpaRepository<MPagamento, Integer> {
}
