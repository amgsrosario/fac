# Legacy UI

This folder is intentionally empty for now.

The current FAC UI remains in its original files and is selected by default through `VITE_FAC_UI_MODE=legacy`.
No functional screen has been moved during the commercial UI foundation task.
